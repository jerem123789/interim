{
  "name": "interim-preselection",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "pnpm -C apps/api dev & pnpm -C apps/web dev",
    "dev:web": "pnpm -C apps/web dev",
    "dev:api": "pnpm -C apps/api dev",
    "dev:mobile": "pnpm -C apps/mobile start",
    "build": "pnpm -C apps/shared build && pnpm -C apps/api build && pnpm -C apps/web build"
  }
}
